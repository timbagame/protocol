/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getAddressEncoder, getBytesDecoder, getBytesEncoder, getProgramDerivedAddress, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, } from "@solana/kit";
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount, } from "@solana/kit/program-client-core";
import { findGameVaultPda, findOraclePda } from "../pdas/index.js";
import { TIMBA_PROGRAM_ADDRESS } from "../programs/index.js";
export const CLOSE_GAME_DISCRIMINATOR = new Uint8Array([
    237, 236, 157, 201, 253, 20, 248, 67,
]);
export function getCloseGameDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(CLOSE_GAME_DISCRIMINATOR);
}
export function getCloseGameInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: CLOSE_GAME_DISCRIMINATOR }));
}
export function getCloseGameInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ]);
}
export function getCloseGameInstructionDataCodec() {
    return combineCodec(getCloseGameInstructionDataEncoder(), getCloseGameInstructionDataDecoder());
}
export async function getCloseGameInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? TIMBA_PROGRAM_ADDRESS;
    // Account meta helper.
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    // Original accounts.
    const originalAccounts = {
        game: { value: input.game ?? null, isSigner: false, isWritable: true },
        creator: { value: input.creator ?? null, isSigner: true, isWritable: true },
        oracle: { value: input.oracle ?? null, isSigner: false, isWritable: false },
        tokenMint: {
            value: input.tokenMint ?? null,
            isSigner: false,
            isWritable: false,
        },
        gameVault: {
            value: input.gameVault ?? null,
            isSigner: false,
            isWritable: false,
        },
        gameVaultTokenAccount: {
            value: input.gameVaultTokenAccount ?? null,
            isSigner: false,
            isWritable: true,
        },
        tokenProgram: {
            value: input.tokenProgram ?? null,
            isSigner: false,
            isWritable: false,
        },
        associatedTokenProgram: {
            value: input.associatedTokenProgram ?? null,
            isSigner: false,
            isWritable: false,
        },
        creatorTokenAccount: {
            value: input.creatorTokenAccount ?? null,
            isSigner: false,
            isWritable: true,
        },
    };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.oracle.value) {
        accounts.oracle.value = await findOraclePda({ programAddress });
    }
    if (!accounts.gameVault.value) {
        accounts.gameVault.value = await findGameVaultPda({
            tokenMint: getAddressFromResolvedInstructionAccount("tokenMint", accounts.tokenMint.value),
        }, { programAddress });
    }
    if (!accounts.tokenProgram.value) {
        accounts.tokenProgram.value =
            "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA";
    }
    if (!accounts.gameVaultTokenAccount.value) {
        accounts.gameVaultTokenAccount.value = await getProgramDerivedAddress({
            programAddress: "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL",
            seeds: [
                getAddressEncoder().encode(getAddressFromResolvedInstructionAccount("gameVault", accounts.gameVault.value)),
                getAddressEncoder().encode(getAddressFromResolvedInstructionAccount("tokenProgram", accounts.tokenProgram.value)),
                getAddressEncoder().encode(getAddressFromResolvedInstructionAccount("tokenMint", accounts.tokenMint.value)),
            ],
        });
    }
    if (!accounts.associatedTokenProgram.value) {
        accounts.associatedTokenProgram.value =
            "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL";
    }
    return Object.freeze({
        accounts: [
            getAccountMeta("game", accounts.game),
            getAccountMeta("creator", accounts.creator),
            getAccountMeta("oracle", accounts.oracle),
            getAccountMeta("tokenMint", accounts.tokenMint),
            getAccountMeta("gameVault", accounts.gameVault),
            getAccountMeta("gameVaultTokenAccount", accounts.gameVaultTokenAccount),
            getAccountMeta("tokenProgram", accounts.tokenProgram),
            getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
            getAccountMeta("creatorTokenAccount", accounts.creatorTokenAccount),
        ],
        data: getCloseGameInstructionDataEncoder().encode({}),
        programAddress,
    });
}
export function getCloseGameInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? TIMBA_PROGRAM_ADDRESS;
    // Account meta helper.
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    // Original accounts.
    const originalAccounts = {
        game: { value: input.game ?? null, isSigner: false, isWritable: true },
        creator: { value: input.creator ?? null, isSigner: true, isWritable: true },
        oracle: { value: input.oracle ?? null, isSigner: false, isWritable: false },
        tokenMint: {
            value: input.tokenMint ?? null,
            isSigner: false,
            isWritable: false,
        },
        gameVault: {
            value: input.gameVault ?? null,
            isSigner: false,
            isWritable: false,
        },
        gameVaultTokenAccount: {
            value: input.gameVaultTokenAccount ?? null,
            isSigner: false,
            isWritable: true,
        },
        tokenProgram: {
            value: input.tokenProgram ?? null,
            isSigner: false,
            isWritable: false,
        },
        associatedTokenProgram: {
            value: input.associatedTokenProgram ?? null,
            isSigner: false,
            isWritable: false,
        },
        creatorTokenAccount: {
            value: input.creatorTokenAccount ?? null,
            isSigner: false,
            isWritable: true,
        },
    };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.tokenProgram.value) {
        accounts.tokenProgram.value =
            "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA";
    }
    if (!accounts.associatedTokenProgram.value) {
        accounts.associatedTokenProgram.value =
            "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL";
    }
    return Object.freeze({
        accounts: [
            getAccountMeta("game", accounts.game),
            getAccountMeta("creator", accounts.creator),
            getAccountMeta("oracle", accounts.oracle),
            getAccountMeta("tokenMint", accounts.tokenMint),
            getAccountMeta("gameVault", accounts.gameVault),
            getAccountMeta("gameVaultTokenAccount", accounts.gameVaultTokenAccount),
            getAccountMeta("tokenProgram", accounts.tokenProgram),
            getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
            getAccountMeta("creatorTokenAccount", accounts.creatorTokenAccount),
        ],
        data: getCloseGameInstructionDataEncoder().encode({}),
        programAddress,
    });
}
export function parseCloseGameInstruction(instruction) {
    if (instruction.accounts.length < 9) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 9,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            game: getNextAccount(),
            creator: getNextAccount(),
            oracle: getNextAccount(),
            tokenMint: getNextAccount(),
            gameVault: getNextAccount(),
            gameVaultTokenAccount: getNextAccount(),
            tokenProgram: getNextAccount(),
            associatedTokenProgram: getNextAccount(),
            creatorTokenAccount: getNextAccount(),
        },
        data: getCloseGameInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=closeGame.js.map