/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type EncodedAccount,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const GAME_TOKEN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  146, 160, 167, 120, 232, 217, 240, 63,
]);

export function getGameTokenDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(GAME_TOKEN_DISCRIMINATOR);
}

export type GameToken = {
  discriminator: ReadonlyUint8Array;
  /** Token mint for this game token configuration */
  tokenMint: Address;
  /** Vault bump seed for PDA token transfers */
  vaultBump: number;
  /** Minimum amount required to participate in games */
  minAmount: bigint;
  /** Accumulated fee amount for this token */
  feeAmount: bigint;
  /** Whether this token is enabled for games */
  enabled: boolean;
};

export type GameTokenArgs = {
  /** Token mint for this game token configuration */
  tokenMint: Address;
  /** Vault bump seed for PDA token transfers */
  vaultBump: number;
  /** Minimum amount required to participate in games */
  minAmount: number | bigint;
  /** Accumulated fee amount for this token */
  feeAmount: number | bigint;
  /** Whether this token is enabled for games */
  enabled: boolean;
};

/** Gets the encoder for {@link GameTokenArgs} account data. */
export function getGameTokenEncoder(): FixedSizeEncoder<GameTokenArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["tokenMint", getAddressEncoder()],
      ["vaultBump", getU8Encoder()],
      ["minAmount", getU64Encoder()],
      ["feeAmount", getU64Encoder()],
      ["enabled", getBooleanEncoder()],
    ]),
    (value) => ({ ...value, discriminator: GAME_TOKEN_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link GameToken} account data. */
export function getGameTokenDecoder(): FixedSizeDecoder<GameToken> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["tokenMint", getAddressDecoder()],
    ["vaultBump", getU8Decoder()],
    ["minAmount", getU64Decoder()],
    ["feeAmount", getU64Decoder()],
    ["enabled", getBooleanDecoder()],
  ]);
}

/** Gets the codec for {@link GameToken} account data. */
export function getGameTokenCodec(): FixedSizeCodec<GameTokenArgs, GameToken> {
  return combineCodec(getGameTokenEncoder(), getGameTokenDecoder());
}

export function decodeGameToken<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<GameToken, TAddress>;
export function decodeGameToken<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<GameToken, TAddress>;
export function decodeGameToken<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<GameToken, TAddress> | MaybeAccount<GameToken, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getGameTokenDecoder(),
  );
}

export async function fetchGameToken<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<GameToken, TAddress>> {
  const maybeAccount = await fetchMaybeGameToken(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeGameToken<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<GameToken, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeGameToken(maybeAccount);
}

export async function fetchAllGameToken(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<GameToken>[]> {
  const maybeAccounts = await fetchAllMaybeGameToken(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeGameToken(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<GameToken>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeGameToken(maybeAccount));
}

export function getGameTokenSize(): number {
  return 58;
}
