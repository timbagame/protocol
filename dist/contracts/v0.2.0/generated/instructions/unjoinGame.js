/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getAddressEncoder, getBytesDecoder, getBytesEncoder, getProgramDerivedAddress, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, } from "@solana/kit";
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount, } from "@solana/kit/program-client-core";
import { findGameTokenPda, findGameVaultPda, findOraclePda, } from "../pdas/index.js";
import { TIMBA_PROGRAM_ADDRESS } from "../programs/index.js";
export const UNJOIN_GAME_DISCRIMINATOR = new Uint8Array([
    105, 123, 174, 143, 207, 2, 17, 206,
]);
export function getUnjoinGameDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(UNJOIN_GAME_DISCRIMINATOR);
}
export function getUnjoinGameInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: UNJOIN_GAME_DISCRIMINATOR }));
}
export function getUnjoinGameInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ]);
}
export function getUnjoinGameInstructionDataCodec() {
    return combineCodec(getUnjoinGameInstructionDataEncoder(), getUnjoinGameInstructionDataDecoder());
}
export async function getUnjoinGameInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? TIMBA_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        game: { value: input.game ?? null, isWritable: true },
        player: { value: input.player ?? null, isWritable: false },
        authority: { value: input.authority ?? null, isWritable: false },
        oracle: { value: input.oracle ?? null, isWritable: false },
        tokenMint: { value: input.tokenMint ?? null, isWritable: false },
        gameToken: { value: input.gameToken ?? null, isWritable: true },
        gameVault: { value: input.gameVault ?? null, isWritable: false },
        gameTokenAccount: {
            value: input.gameTokenAccount ?? null,
            isWritable: true,
        },
        tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
        associatedTokenProgram: {
            value: input.associatedTokenProgram ?? null,
            isWritable: false,
        },
        playerTokenAccount: {
            value: input.playerTokenAccount ?? null,
            isWritable: true,
        },
    };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.oracle.value) {
        accounts.oracle.value = await findOraclePda();
    }
    if (!accounts.gameToken.value) {
        accounts.gameToken.value = await findGameTokenPda({
            tokenMint: getAddressFromResolvedInstructionAccount("tokenMint", accounts.tokenMint.value),
        });
    }
    if (!accounts.gameVault.value) {
        accounts.gameVault.value = await findGameVaultPda({
            tokenMint: getAddressFromResolvedInstructionAccount("tokenMint", accounts.tokenMint.value),
        });
    }
    if (!accounts.tokenProgram.value) {
        accounts.tokenProgram.value =
            "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA";
    }
    if (!accounts.gameTokenAccount.value) {
        accounts.gameTokenAccount.value = await getProgramDerivedAddress({
            programAddress: "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL",
            seeds: [
                getAddressEncoder().encode(getAddressFromResolvedInstructionAccount("gameVault", accounts.gameVault.value)),
                getAddressEncoder().encode(getAddressFromResolvedInstructionAccount("tokenProgram", accounts.tokenProgram.value)),
                getAddressEncoder().encode(getAddressFromResolvedInstructionAccount("tokenMint", accounts.tokenMint.value)),
            ],
        });
    }
    if (!accounts.associatedTokenProgram.value) {
        accounts.associatedTokenProgram.value =
            "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL";
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta("game", accounts.game),
            getAccountMeta("player", accounts.player),
            getAccountMeta("authority", accounts.authority),
            getAccountMeta("oracle", accounts.oracle),
            getAccountMeta("tokenMint", accounts.tokenMint),
            getAccountMeta("gameToken", accounts.gameToken),
            getAccountMeta("gameVault", accounts.gameVault),
            getAccountMeta("gameTokenAccount", accounts.gameTokenAccount),
            getAccountMeta("tokenProgram", accounts.tokenProgram),
            getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
            getAccountMeta("playerTokenAccount", accounts.playerTokenAccount),
        ],
        data: getUnjoinGameInstructionDataEncoder().encode({}),
        programAddress,
    });
}
export function getUnjoinGameInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? TIMBA_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        game: { value: input.game ?? null, isWritable: true },
        player: { value: input.player ?? null, isWritable: false },
        authority: { value: input.authority ?? null, isWritable: false },
        oracle: { value: input.oracle ?? null, isWritable: false },
        tokenMint: { value: input.tokenMint ?? null, isWritable: false },
        gameToken: { value: input.gameToken ?? null, isWritable: true },
        gameVault: { value: input.gameVault ?? null, isWritable: false },
        gameTokenAccount: {
            value: input.gameTokenAccount ?? null,
            isWritable: true,
        },
        tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
        associatedTokenProgram: {
            value: input.associatedTokenProgram ?? null,
            isWritable: false,
        },
        playerTokenAccount: {
            value: input.playerTokenAccount ?? null,
            isWritable: true,
        },
    };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.tokenProgram.value) {
        accounts.tokenProgram.value =
            "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA";
    }
    if (!accounts.associatedTokenProgram.value) {
        accounts.associatedTokenProgram.value =
            "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL";
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta("game", accounts.game),
            getAccountMeta("player", accounts.player),
            getAccountMeta("authority", accounts.authority),
            getAccountMeta("oracle", accounts.oracle),
            getAccountMeta("tokenMint", accounts.tokenMint),
            getAccountMeta("gameToken", accounts.gameToken),
            getAccountMeta("gameVault", accounts.gameVault),
            getAccountMeta("gameTokenAccount", accounts.gameTokenAccount),
            getAccountMeta("tokenProgram", accounts.tokenProgram),
            getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
            getAccountMeta("playerTokenAccount", accounts.playerTokenAccount),
        ],
        data: getUnjoinGameInstructionDataEncoder().encode({}),
        programAddress,
    });
}
export function parseUnjoinGameInstruction(instruction) {
    if (instruction.accounts.length < 11) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 11,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            game: getNextAccount(),
            player: getNextAccount(),
            authority: getNextAccount(),
            oracle: getNextAccount(),
            tokenMint: getNextAccount(),
            gameToken: getNextAccount(),
            gameVault: getNextAccount(),
            gameTokenAccount: getNextAccount(),
            tokenProgram: getNextAccount(),
            associatedTokenProgram: getNextAccount(),
            playerTokenAccount: getNextAccount(),
        },
        data: getUnjoinGameInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=unjoinGame.js.map