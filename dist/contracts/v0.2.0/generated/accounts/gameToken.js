/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBooleanDecoder, getBooleanEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, getU8Decoder, getU8Encoder, transformEncoder, } from "@solana/kit";
export const GAME_TOKEN_DISCRIMINATOR = new Uint8Array([
    146, 160, 167, 120, 232, 217, 240, 63,
]);
export function getGameTokenDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(GAME_TOKEN_DISCRIMINATOR);
}
/** Gets the encoder for {@link GameTokenArgs} account data. */
export function getGameTokenEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["tokenMint", getAddressEncoder()],
        ["vaultBump", getU8Encoder()],
        ["minAmount", getU64Encoder()],
        ["feeAmount", getU64Encoder()],
        ["enabled", getBooleanEncoder()],
    ]), (value) => ({ ...value, discriminator: GAME_TOKEN_DISCRIMINATOR }));
}
/** Gets the decoder for {@link GameToken} account data. */
export function getGameTokenDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["tokenMint", getAddressDecoder()],
        ["vaultBump", getU8Decoder()],
        ["minAmount", getU64Decoder()],
        ["feeAmount", getU64Decoder()],
        ["enabled", getBooleanDecoder()],
    ]);
}
/** Gets the codec for {@link GameToken} account data. */
export function getGameTokenCodec() {
    return combineCodec(getGameTokenEncoder(), getGameTokenDecoder());
}
export function decodeGameToken(encodedAccount) {
    return decodeAccount(encodedAccount, getGameTokenDecoder());
}
export async function fetchGameToken(rpc, address, config) {
    const maybeAccount = await fetchMaybeGameToken(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}
export async function fetchMaybeGameToken(rpc, address, config) {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeGameToken(maybeAccount);
}
export async function fetchAllGameToken(rpc, addresses, config) {
    const maybeAccounts = await fetchAllMaybeGameToken(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}
export async function fetchAllMaybeGameToken(rpc, addresses, config) {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map((maybeAccount) => decodeGameToken(maybeAccount));
}
export function getGameTokenSize() {
    return 58;
}
//# sourceMappingURL=gameToken.js.map