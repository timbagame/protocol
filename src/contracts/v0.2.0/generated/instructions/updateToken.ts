/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/kit/program-client-core";
import { findGameTokenPda, findOraclePda } from "../pdas/index.js";
import { TIMBA_PROGRAM_ADDRESS } from "../programs/index.js";
import {
  getTokenConfigDecoder,
  getTokenConfigEncoder,
  type TokenConfig,
  type TokenConfigArgs,
} from "../types/index.js";

export const UPDATE_TOKEN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  92, 200, 25, 239, 138, 254, 58, 102,
]);

export function getUpdateTokenDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_TOKEN_DISCRIMINATOR,
  );
}

export type UpdateTokenInstruction<
  TProgram extends string = typeof TIMBA_PROGRAM_ADDRESS,
  TAccountGameToken extends string | AccountMeta<string> = string,
  TAccountTokenMint extends string | AccountMeta<string> = string,
  TAccountOracle extends string | AccountMeta<string> = string,
  TAccountOracleOperator extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountGameToken extends string
        ? WritableAccount<TAccountGameToken>
        : TAccountGameToken,
      TAccountTokenMint extends string
        ? ReadonlyAccount<TAccountTokenMint>
        : TAccountTokenMint,
      TAccountOracle extends string
        ? ReadonlyAccount<TAccountOracle>
        : TAccountOracle,
      TAccountOracleOperator extends string
        ? ReadonlySignerAccount<TAccountOracleOperator> &
            AccountSignerMeta<TAccountOracleOperator>
        : TAccountOracleOperator,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateTokenInstructionData = {
  discriminator: ReadonlyUint8Array;
  config: TokenConfig;
};

export type UpdateTokenInstructionDataArgs = { config: TokenConfigArgs };

export function getUpdateTokenInstructionDataEncoder(): FixedSizeEncoder<UpdateTokenInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["config", getTokenConfigEncoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_TOKEN_DISCRIMINATOR }),
  );
}

export function getUpdateTokenInstructionDataDecoder(): FixedSizeDecoder<UpdateTokenInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["config", getTokenConfigDecoder()],
  ]);
}

export function getUpdateTokenInstructionDataCodec(): FixedSizeCodec<
  UpdateTokenInstructionDataArgs,
  UpdateTokenInstructionData
> {
  return combineCodec(
    getUpdateTokenInstructionDataEncoder(),
    getUpdateTokenInstructionDataDecoder(),
  );
}

export type UpdateTokenAsyncInput<
  TAccountGameToken extends string = string,
  TAccountTokenMint extends string = string,
  TAccountOracle extends string = string,
  TAccountOracleOperator extends string = string,
> = {
  gameToken?: Address<TAccountGameToken>;
  tokenMint: Address<TAccountTokenMint>;
  oracle?: Address<TAccountOracle>;
  oracleOperator: TransactionSigner<TAccountOracleOperator>;
  config: UpdateTokenInstructionDataArgs["config"];
};

export async function getUpdateTokenInstructionAsync<
  TAccountGameToken extends string,
  TAccountTokenMint extends string,
  TAccountOracle extends string,
  TAccountOracleOperator extends string,
  TProgramAddress extends Address = typeof TIMBA_PROGRAM_ADDRESS,
>(
  input: UpdateTokenAsyncInput<
    TAccountGameToken,
    TAccountTokenMint,
    TAccountOracle,
    TAccountOracleOperator
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  UpdateTokenInstruction<
    TProgramAddress,
    TAccountGameToken,
    TAccountTokenMint,
    TAccountOracle,
    TAccountOracleOperator
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? TIMBA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    gameToken: { value: input.gameToken ?? null, isWritable: true },
    tokenMint: { value: input.tokenMint ?? null, isWritable: false },
    oracle: { value: input.oracle ?? null, isWritable: false },
    oracleOperator: { value: input.oracleOperator ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.gameToken.value) {
    accounts.gameToken.value = await findGameTokenPda(
      {
        tokenMint: getAddressFromResolvedInstructionAccount(
          "tokenMint",
          accounts.tokenMint.value,
        ),
      },
      { programAddress },
    );
  }
  if (!accounts.oracle.value) {
    accounts.oracle.value = await findOraclePda({ programAddress });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("gameToken", accounts.gameToken),
      getAccountMeta("tokenMint", accounts.tokenMint),
      getAccountMeta("oracle", accounts.oracle),
      getAccountMeta("oracleOperator", accounts.oracleOperator),
    ],
    data: getUpdateTokenInstructionDataEncoder().encode(
      args as UpdateTokenInstructionDataArgs,
    ),
    programAddress,
  } as UpdateTokenInstruction<
    TProgramAddress,
    TAccountGameToken,
    TAccountTokenMint,
    TAccountOracle,
    TAccountOracleOperator
  >);
}

export type UpdateTokenInput<
  TAccountGameToken extends string = string,
  TAccountTokenMint extends string = string,
  TAccountOracle extends string = string,
  TAccountOracleOperator extends string = string,
> = {
  gameToken: Address<TAccountGameToken>;
  tokenMint: Address<TAccountTokenMint>;
  oracle: Address<TAccountOracle>;
  oracleOperator: TransactionSigner<TAccountOracleOperator>;
  config: UpdateTokenInstructionDataArgs["config"];
};

export function getUpdateTokenInstruction<
  TAccountGameToken extends string,
  TAccountTokenMint extends string,
  TAccountOracle extends string,
  TAccountOracleOperator extends string,
  TProgramAddress extends Address = typeof TIMBA_PROGRAM_ADDRESS,
>(
  input: UpdateTokenInput<
    TAccountGameToken,
    TAccountTokenMint,
    TAccountOracle,
    TAccountOracleOperator
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateTokenInstruction<
  TProgramAddress,
  TAccountGameToken,
  TAccountTokenMint,
  TAccountOracle,
  TAccountOracleOperator
> {
  // Program address.
  const programAddress = config?.programAddress ?? TIMBA_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    gameToken: { value: input.gameToken ?? null, isWritable: true },
    tokenMint: { value: input.tokenMint ?? null, isWritable: false },
    oracle: { value: input.oracle ?? null, isWritable: false },
    oracleOperator: { value: input.oracleOperator ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("gameToken", accounts.gameToken),
      getAccountMeta("tokenMint", accounts.tokenMint),
      getAccountMeta("oracle", accounts.oracle),
      getAccountMeta("oracleOperator", accounts.oracleOperator),
    ],
    data: getUpdateTokenInstructionDataEncoder().encode(
      args as UpdateTokenInstructionDataArgs,
    ),
    programAddress,
  } as UpdateTokenInstruction<
    TProgramAddress,
    TAccountGameToken,
    TAccountTokenMint,
    TAccountOracle,
    TAccountOracleOperator
  >);
}

export type ParsedUpdateTokenInstruction<
  TProgram extends string = typeof TIMBA_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    gameToken: TAccountMetas[0];
    tokenMint: TAccountMetas[1];
    oracle: TAccountMetas[2];
    oracleOperator: TAccountMetas[3];
  };
  data: UpdateTokenInstructionData;
};

export function parseUpdateTokenInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateTokenInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      gameToken: getNextAccount(),
      tokenMint: getNextAccount(),
      oracle: getNextAccount(),
      oracleOperator: getNextAccount(),
    },
    data: getUpdateTokenInstructionDataDecoder().decode(instruction.data),
  };
}
