/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, } from "@solana/kit";
import { getAccountMetaFactory, } from "@solana/kit/program-client-core";
import { findOraclePda } from "../pdas/index.js";
import { TIMBA_PROGRAM_ADDRESS } from "../programs/index.js";
import { getOracleConfigDecoder, getOracleConfigEncoder, } from "../types/index.js";
export const UPDATE_ORACLE_DISCRIMINATOR = new Uint8Array([
    112, 41, 209, 18, 248, 226, 252, 188,
]);
export function getUpdateOracleDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_ORACLE_DISCRIMINATOR);
}
export function getUpdateOracleInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["config", getOracleConfigEncoder()],
    ]), (value) => ({ ...value, discriminator: UPDATE_ORACLE_DISCRIMINATOR }));
}
export function getUpdateOracleInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["config", getOracleConfigDecoder()],
    ]);
}
export function getUpdateOracleInstructionDataCodec() {
    return combineCodec(getUpdateOracleInstructionDataEncoder(), getUpdateOracleInstructionDataDecoder());
}
export async function getUpdateOracleInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? TIMBA_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        oracle: { value: input.oracle ?? null, isWritable: true },
        oldOracleOperator: {
            value: input.oldOracleOperator ?? null,
            isWritable: false,
        },
        newOracleOperator: {
            value: input.newOracleOperator ?? null,
            isWritable: false,
        },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    // Resolve default values.
    if (!accounts.oracle.value) {
        accounts.oracle.value = await findOraclePda();
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta("oracle", accounts.oracle),
            getAccountMeta("oldOracleOperator", accounts.oldOracleOperator),
            getAccountMeta("newOracleOperator", accounts.newOracleOperator),
        ],
        data: getUpdateOracleInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function getUpdateOracleInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? TIMBA_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        oracle: { value: input.oracle ?? null, isWritable: true },
        oldOracleOperator: {
            value: input.oldOracleOperator ?? null,
            isWritable: false,
        },
        newOracleOperator: {
            value: input.newOracleOperator ?? null,
            isWritable: false,
        },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta("oracle", accounts.oracle),
            getAccountMeta("oldOracleOperator", accounts.oldOracleOperator),
            getAccountMeta("newOracleOperator", accounts.newOracleOperator),
        ],
        data: getUpdateOracleInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseUpdateOracleInstruction(instruction) {
    if (instruction.accounts.length < 3) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 3,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            oracle: getNextAccount(),
            oldOracleOperator: getNextAccount(),
            newOracleOperator: getNextAccount(),
        },
        data: getUpdateOracleInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=updateOracle.js.map