/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, } from "@solana/kit";
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount, } from "@solana/kit/program-client-core";
import { findGameTokenPda, findOraclePda } from "../pdas/index.js";
import { TIMBA_PROGRAM_ADDRESS } from "../programs/index.js";
import { getTokenConfigDecoder, getTokenConfigEncoder, } from "../types/index.js";
export const UPDATE_TOKEN_DISCRIMINATOR = new Uint8Array([
    92, 200, 25, 239, 138, 254, 58, 102,
]);
export function getUpdateTokenDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_TOKEN_DISCRIMINATOR);
}
export function getUpdateTokenInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["config", getTokenConfigEncoder()],
    ]), (value) => ({ ...value, discriminator: UPDATE_TOKEN_DISCRIMINATOR }));
}
export function getUpdateTokenInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["config", getTokenConfigDecoder()],
    ]);
}
export function getUpdateTokenInstructionDataCodec() {
    return combineCodec(getUpdateTokenInstructionDataEncoder(), getUpdateTokenInstructionDataDecoder());
}
export async function getUpdateTokenInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? TIMBA_PROGRAM_ADDRESS;
    // Account meta helper.
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    // Original accounts.
    const originalAccounts = {
        gameToken: {
            value: input.gameToken ?? null,
            isSigner: false,
            isWritable: true,
        },
        tokenMint: {
            value: input.tokenMint ?? null,
            isSigner: false,
            isWritable: false,
        },
        oracle: { value: input.oracle ?? null, isSigner: false, isWritable: false },
        oracleOperator: {
            value: input.oracleOperator ?? null,
            isSigner: true,
            isWritable: false,
        },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    // Resolve default values.
    if (!accounts.gameToken.value) {
        accounts.gameToken.value = await findGameTokenPda({
            tokenMint: getAddressFromResolvedInstructionAccount("tokenMint", accounts.tokenMint.value),
        }, { programAddress });
    }
    if (!accounts.oracle.value) {
        accounts.oracle.value = await findOraclePda({ programAddress });
    }
    return Object.freeze({
        accounts: [
            getAccountMeta("gameToken", accounts.gameToken),
            getAccountMeta("tokenMint", accounts.tokenMint),
            getAccountMeta("oracle", accounts.oracle),
            getAccountMeta("oracleOperator", accounts.oracleOperator),
        ],
        data: getUpdateTokenInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function getUpdateTokenInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? TIMBA_PROGRAM_ADDRESS;
    // Account meta helper.
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    // Original accounts.
    const originalAccounts = {
        gameToken: {
            value: input.gameToken ?? null,
            isSigner: false,
            isWritable: true,
        },
        tokenMint: {
            value: input.tokenMint ?? null,
            isSigner: false,
            isWritable: false,
        },
        oracle: { value: input.oracle ?? null, isSigner: false, isWritable: false },
        oracleOperator: {
            value: input.oracleOperator ?? null,
            isSigner: true,
            isWritable: false,
        },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    return Object.freeze({
        accounts: [
            getAccountMeta("gameToken", accounts.gameToken),
            getAccountMeta("tokenMint", accounts.tokenMint),
            getAccountMeta("oracle", accounts.oracle),
            getAccountMeta("oracleOperator", accounts.oracleOperator),
        ],
        data: getUpdateTokenInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseUpdateTokenInstruction(instruction) {
    if (instruction.accounts.length < 4) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 4,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            gameToken: getNextAccount(),
            tokenMint: getNextAccount(),
            oracle: getNextAccount(),
            oracleOperator: getNextAccount(),
        },
        data: getUpdateTokenInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=updateToken.js.map