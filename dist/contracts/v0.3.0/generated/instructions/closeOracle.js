/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, } from "@solana/kit";
import { getAccountMetaFactory, } from "@solana/kit/program-client-core";
import { findOraclePda } from "../pdas/index.js";
import { TIMBA_PROGRAM_ADDRESS } from "../programs/index.js";
export const CLOSE_ORACLE_DISCRIMINATOR = new Uint8Array([
    74, 239, 49, 223, 206, 52, 189, 123,
]);
export function getCloseOracleDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(CLOSE_ORACLE_DISCRIMINATOR);
}
export function getCloseOracleInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: CLOSE_ORACLE_DISCRIMINATOR }));
}
export function getCloseOracleInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ]);
}
export function getCloseOracleInstructionDataCodec() {
    return combineCodec(getCloseOracleInstructionDataEncoder(), getCloseOracleInstructionDataDecoder());
}
export async function getCloseOracleInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? TIMBA_PROGRAM_ADDRESS;
    // Account meta helper.
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    // Original accounts.
    const originalAccounts = {
        oracle: { value: input.oracle ?? null, isSigner: false, isWritable: true },
        oracleOperator: {
            value: input.oracleOperator ?? null,
            isSigner: true,
            isWritable: true,
        },
        upgradeAuthority: {
            value: input.upgradeAuthority ?? null,
            isSigner: true,
            isWritable: false,
        },
        program: {
            value: input.program ?? null,
            isSigner: false,
            isWritable: false,
        },
        programData: {
            value: input.programData ?? null,
            isSigner: false,
            isWritable: false,
        },
    };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.oracle.value) {
        accounts.oracle.value = await findOraclePda({ programAddress });
    }
    if (!accounts.program.value) {
        accounts.program.value =
            "32Jr4JnXWvqq9GqPQynkooHsszaucUUvZfNLh2hdX2L5";
    }
    return Object.freeze({
        accounts: [
            getAccountMeta("oracle", accounts.oracle),
            getAccountMeta("oracleOperator", accounts.oracleOperator),
            getAccountMeta("upgradeAuthority", accounts.upgradeAuthority),
            getAccountMeta("program", accounts.program),
            getAccountMeta("programData", accounts.programData),
        ],
        data: getCloseOracleInstructionDataEncoder().encode({}),
        programAddress,
    });
}
export function getCloseOracleInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? TIMBA_PROGRAM_ADDRESS;
    // Account meta helper.
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    // Original accounts.
    const originalAccounts = {
        oracle: { value: input.oracle ?? null, isSigner: false, isWritable: true },
        oracleOperator: {
            value: input.oracleOperator ?? null,
            isSigner: true,
            isWritable: true,
        },
        upgradeAuthority: {
            value: input.upgradeAuthority ?? null,
            isSigner: true,
            isWritable: false,
        },
        program: {
            value: input.program ?? null,
            isSigner: false,
            isWritable: false,
        },
        programData: {
            value: input.programData ?? null,
            isSigner: false,
            isWritable: false,
        },
    };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.program.value) {
        accounts.program.value =
            "32Jr4JnXWvqq9GqPQynkooHsszaucUUvZfNLh2hdX2L5";
    }
    return Object.freeze({
        accounts: [
            getAccountMeta("oracle", accounts.oracle),
            getAccountMeta("oracleOperator", accounts.oracleOperator),
            getAccountMeta("upgradeAuthority", accounts.upgradeAuthority),
            getAccountMeta("program", accounts.program),
            getAccountMeta("programData", accounts.programData),
        ],
        data: getCloseOracleInstructionDataEncoder().encode({}),
        programAddress,
    });
}
export function parseCloseOracleInstruction(instruction) {
    if (instruction.accounts.length < 5) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 5,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            oracle: getNextAccount(),
            oracleOperator: getNextAccount(),
            upgradeAuthority: getNextAccount(),
            program: getNextAccount(),
            programData: getNextAccount(),
        },
        data: getCloseOracleInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=closeOracle.js.map