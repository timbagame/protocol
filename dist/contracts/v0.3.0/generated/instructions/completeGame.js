/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getAddressEncoder, getBytesDecoder, getBytesEncoder, getProgramDerivedAddress, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, SolanaError, transformEncoder, } from "@solana/kit";
import { getAccountMetaFactory, getAddressFromResolvedInstructionAccount, getNonNullResolvedInstructionInput, } from "@solana/kit/program-client-core";
import { findGamePda, findGameVaultPda, findOraclePda } from "../pdas/index.js";
import { TIMBA_PROGRAM_ADDRESS } from "../programs/index.js";
export const COMPLETE_GAME_DISCRIMINATOR = new Uint8Array([
    105, 69, 184, 5, 143, 182, 92, 132,
]);
export function getCompleteGameDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(COMPLETE_GAME_DISCRIMINATOR);
}
export function getCompleteGameInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["randomHash", fixEncoderSize(getBytesEncoder(), 32)],
        ["secretKey", fixEncoderSize(getBytesEncoder(), 32)],
        ["winnerIndex", getU32Encoder()],
    ]), (value) => ({ ...value, discriminator: COMPLETE_GAME_DISCRIMINATOR }));
}
export function getCompleteGameInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["randomHash", fixDecoderSize(getBytesDecoder(), 32)],
        ["secretKey", fixDecoderSize(getBytesDecoder(), 32)],
        ["winnerIndex", getU32Decoder()],
    ]);
}
export function getCompleteGameInstructionDataCodec() {
    return combineCodec(getCompleteGameInstructionDataEncoder(), getCompleteGameInstructionDataDecoder());
}
export async function getCompleteGameInstructionAsync(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? TIMBA_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        game: { value: input.game ?? null, isWritable: true },
        tokenMint: { value: input.tokenMint ?? null, isWritable: false },
        gameVault: { value: input.gameVault ?? null, isWritable: false },
        gameVaultTokenAccount: {
            value: input.gameVaultTokenAccount ?? null,
            isWritable: true,
        },
        tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
        associatedTokenProgram: {
            value: input.associatedTokenProgram ?? null,
            isWritable: false,
        },
        oracle: { value: input.oracle ?? null, isWritable: false },
        oracleOperator: { value: input.oracleOperator ?? null, isWritable: false },
        winner: { value: input.winner ?? null, isWritable: false },
        creator: { value: input.creator ?? null, isWritable: true },
        winnerTokenAccount: {
            value: input.winnerTokenAccount ?? null,
            isWritable: true,
        },
        oracleOperatorTokenAccount: {
            value: input.oracleOperatorTokenAccount ?? null,
            isWritable: true,
        },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    // Resolve default values.
    if (!accounts.game.value) {
        accounts.game.value = await findGamePda({
            randomHash: getNonNullResolvedInstructionInput("randomHash", args.randomHash),
        }, { programAddress });
    }
    if (!accounts.gameVault.value) {
        accounts.gameVault.value = await findGameVaultPda({
            tokenMint: getAddressFromResolvedInstructionAccount("tokenMint", accounts.tokenMint.value),
        }, { programAddress });
    }
    if (!accounts.tokenProgram.value) {
        accounts.tokenProgram.value =
            "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA";
    }
    if (!accounts.gameVaultTokenAccount.value) {
        accounts.gameVaultTokenAccount.value = await getProgramDerivedAddress({
            programAddress: "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL",
            seeds: [
                getAddressEncoder().encode(getAddressFromResolvedInstructionAccount("gameVault", accounts.gameVault.value)),
                getAddressEncoder().encode(getAddressFromResolvedInstructionAccount("tokenProgram", accounts.tokenProgram.value)),
                getAddressEncoder().encode(getAddressFromResolvedInstructionAccount("tokenMint", accounts.tokenMint.value)),
            ],
        });
    }
    if (!accounts.associatedTokenProgram.value) {
        accounts.associatedTokenProgram.value =
            "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL";
    }
    if (!accounts.oracle.value) {
        accounts.oracle.value = await findOraclePda({ programAddress });
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta("game", accounts.game),
            getAccountMeta("tokenMint", accounts.tokenMint),
            getAccountMeta("gameVault", accounts.gameVault),
            getAccountMeta("gameVaultTokenAccount", accounts.gameVaultTokenAccount),
            getAccountMeta("tokenProgram", accounts.tokenProgram),
            getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
            getAccountMeta("oracle", accounts.oracle),
            getAccountMeta("oracleOperator", accounts.oracleOperator),
            getAccountMeta("winner", accounts.winner),
            getAccountMeta("creator", accounts.creator),
            getAccountMeta("winnerTokenAccount", accounts.winnerTokenAccount),
            getAccountMeta("oracleOperatorTokenAccount", accounts.oracleOperatorTokenAccount),
        ],
        data: getCompleteGameInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function getCompleteGameInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? TIMBA_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        game: { value: input.game ?? null, isWritable: true },
        tokenMint: { value: input.tokenMint ?? null, isWritable: false },
        gameVault: { value: input.gameVault ?? null, isWritable: false },
        gameVaultTokenAccount: {
            value: input.gameVaultTokenAccount ?? null,
            isWritable: true,
        },
        tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
        associatedTokenProgram: {
            value: input.associatedTokenProgram ?? null,
            isWritable: false,
        },
        oracle: { value: input.oracle ?? null, isWritable: false },
        oracleOperator: { value: input.oracleOperator ?? null, isWritable: false },
        winner: { value: input.winner ?? null, isWritable: false },
        creator: { value: input.creator ?? null, isWritable: true },
        winnerTokenAccount: {
            value: input.winnerTokenAccount ?? null,
            isWritable: true,
        },
        oracleOperatorTokenAccount: {
            value: input.oracleOperatorTokenAccount ?? null,
            isWritable: true,
        },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    // Resolve default values.
    if (!accounts.tokenProgram.value) {
        accounts.tokenProgram.value =
            "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA";
    }
    if (!accounts.associatedTokenProgram.value) {
        accounts.associatedTokenProgram.value =
            "ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL";
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta("game", accounts.game),
            getAccountMeta("tokenMint", accounts.tokenMint),
            getAccountMeta("gameVault", accounts.gameVault),
            getAccountMeta("gameVaultTokenAccount", accounts.gameVaultTokenAccount),
            getAccountMeta("tokenProgram", accounts.tokenProgram),
            getAccountMeta("associatedTokenProgram", accounts.associatedTokenProgram),
            getAccountMeta("oracle", accounts.oracle),
            getAccountMeta("oracleOperator", accounts.oracleOperator),
            getAccountMeta("winner", accounts.winner),
            getAccountMeta("creator", accounts.creator),
            getAccountMeta("winnerTokenAccount", accounts.winnerTokenAccount),
            getAccountMeta("oracleOperatorTokenAccount", accounts.oracleOperatorTokenAccount),
        ],
        data: getCompleteGameInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseCompleteGameInstruction(instruction) {
    if (instruction.accounts.length < 12) {
        throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
            actualAccountMetas: instruction.accounts.length,
            expectedAccountMetas: 12,
        });
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            game: getNextAccount(),
            tokenMint: getNextAccount(),
            gameVault: getNextAccount(),
            gameVaultTokenAccount: getNextAccount(),
            tokenProgram: getNextAccount(),
            associatedTokenProgram: getNextAccount(),
            oracle: getNextAccount(),
            oracleOperator: getNextAccount(),
            winner: getNextAccount(),
            creator: getNextAccount(),
            winnerTokenAccount: getNextAccount(),
            oracleOperatorTokenAccount: getNextAccount(),
        },
        data: getCompleteGameInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=completeGame.js.map