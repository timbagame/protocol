/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getArrayDecoder, getArrayEncoder, getBooleanDecoder, getBooleanEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getU64Decoder, getU64Encoder, transformEncoder, } from "@solana/kit";
import { getGameTypeDecoder, getGameTypeEncoder, } from "../types/index.js";
export const GAME_DISCRIMINATOR = new Uint8Array([
    27, 90, 166, 125, 74, 100, 121, 18,
]);
export function getGameDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(GAME_DISCRIMINATOR);
}
/** Gets the encoder for {@link GameArgs} account data. */
export function getGameEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["creator", getAddressEncoder()],
        ["gameType", getGameTypeEncoder()],
        ["ticketAmount", getU64Encoder()],
        ["maxTickets", getU32Encoder()],
        ["minTickets", getU32Encoder()],
        ["ticketsCount", getU32Encoder()],
        ["tokenMint", getAddressEncoder()],
        ["createdAt", getU64Encoder()],
        ["timeout", getU64Encoder()],
        ["lastSlot", getU64Encoder()],
        ["isPrivate", getBooleanEncoder()],
        ["totalAmount", getU64Encoder()],
        ["participants", getArrayEncoder(getAddressEncoder())],
    ]), (value) => ({ ...value, discriminator: GAME_DISCRIMINATOR }));
}
/** Gets the decoder for {@link Game} account data. */
export function getGameDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["creator", getAddressDecoder()],
        ["gameType", getGameTypeDecoder()],
        ["ticketAmount", getU64Decoder()],
        ["maxTickets", getU32Decoder()],
        ["minTickets", getU32Decoder()],
        ["ticketsCount", getU32Decoder()],
        ["tokenMint", getAddressDecoder()],
        ["createdAt", getU64Decoder()],
        ["timeout", getU64Decoder()],
        ["lastSlot", getU64Decoder()],
        ["isPrivate", getBooleanDecoder()],
        ["totalAmount", getU64Decoder()],
        ["participants", getArrayDecoder(getAddressDecoder())],
    ]);
}
/** Gets the codec for {@link Game} account data. */
export function getGameCodec() {
    return combineCodec(getGameEncoder(), getGameDecoder());
}
export function decodeGame(encodedAccount) {
    return decodeAccount(encodedAccount, getGameDecoder());
}
export async function fetchGame(rpc, address, config) {
    const maybeAccount = await fetchMaybeGame(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}
export async function fetchMaybeGame(rpc, address, config) {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeGame(maybeAccount);
}
export async function fetchAllGame(rpc, addresses, config) {
    const maybeAccounts = await fetchAllMaybeGame(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}
export async function fetchAllMaybeGame(rpc, addresses, config) {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map((maybeAccount) => decodeGame(maybeAccount));
}
//# sourceMappingURL=game.js.map